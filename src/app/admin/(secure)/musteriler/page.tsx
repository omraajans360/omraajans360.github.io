import Link from'next/link';import{createClientAction}from'@/lib/admin/actions';import{select}from'@/lib/admin/db';import type{Client}from'@/lib/admin/types';export default async function Page(){const clients=await select<Client>('clients','select=*&order=created_at.desc&limit=200');return <><header className="admin-head"><div><h1>Müşteriler</h1><p>Müşteri bilgileri, geçmiş işler, teklifler ve QR Menü kayıtları.</p></div><div className="admin-note">Toplam {clients.length} kayıt</div></header><section className="admin-card"><h2>Yeni müşteri</h2><form action={createClientAction} className="admin-form"><label>Ad / Yetkili<input name="name" required placeholder="Örn. Ayşe Yılmaz"/></label><label>İşletme<input name="company" placeholder="Örn. Luna Cafe"/></label><label>Telefon<input name="phone" placeholder="05xx xxx xx xx"/></label><label>E-posta<input name="email" type="email" placeholder="ornek@firma.com"/></label><label>Konum<input name="location" placeholder="İzmir / Urla"/></label><label>Not<textarea name="notes" placeholder="Müşteriyle ilgili önemli notlar"/></label><div className="admin-span-2"><button className="admin-btn">Müşteri Kaydını Oluştur</button></div></form></section><section className="admin-section"><div className="admin-section-title"><h2>Müşteri Listesi</h2><span className="admin-note">Detay sayfasından geçmiş hareketleri görebilirsin.</span></div>{clients.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Müşteri</th><th>Telefon</th><th>E-posta</th><th>Konum</th><th>Not</th><th></th></tr></thead><tbody>{clients.map(c=><tr key={c.id}><td><b>{c.name}</b><div className="admin-note">{c.company||'Bireysel müşteri'}</div></td><td>{c.phone||'—'}</td><td>{c.email||'—'}</td><td>{c.location||'—'}</td><td>{c.notes||'—'}</td><td><Link className="admin-btn admin-btn-secondary" href={`/admin/musteriler/${c.id}`}>Detayı Aç</Link></td></tr>)}</tbody></table></div>:<div className="admin-empty"><div><b>Henüz müşteri kaydı yok</b><p>Yukarıdaki formdan ilk müşteriyi eklediğinde müşteri geçmişi burada oluşacak.</p></div></div>}</section></>}
