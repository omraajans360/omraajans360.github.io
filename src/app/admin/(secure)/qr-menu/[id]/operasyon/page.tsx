import Link from'next/link';import{notFound}from'next/navigation';import{QrCodePanel}from'@/components/admin/qr-code-panel';import{QrSetupPaymentPanel}from'@/components/admin/qr-setup-payment-panel';import{QrRenewalPanel}from'@/components/admin/qr-renewal-panel';import{select}from'@/lib/admin/db';import type{QrMenu}from'@/lib/admin/types';
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const menus=await select<QrMenu>('qr_menus',`id=eq.${encodeURIComponent(id)}&select=*`),menu=menus[0];if(!menu)notFound();const views=await select<{id:number}>('menu_views',`menu_id=eq.${encodeURIComponent(id)}&select=id&limit=10000`);return <><header className="admin-head"><div><Link href="/admin/qr-menu" className="admin-note">← QR Menüler</Link><h1>{menu.business_name} · Operasyon</h1><p>QR kodu, görüntülenme, ilk satış tahsilatı ve yıllık yenileme.</p></div><div className="admin-actions"><Link className="admin-btn admin-btn-secondary" href={`/admin/qr-menu/${menu.id}`}>Menü İçeriği</Link><a className="admin-btn" href={`/menu/${menu.slug}`} target="_blank" rel="noreferrer">Menüyü Gör</a></div></header><QrCodePanel menu={menu} views={views.length}/><QrSetupPaymentPanel menu={menu}/><QrRenewalPanel menu={menu}/></>}
