import Link from'next/link';import{ConfirmSubmitButton}from'@/components/admin/confirm-submit-button';import{createQrMenuConfiguredAction,deleteQrMenuAction}from'@/lib/admin/qr-admin-actions';import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{QrMenu,QrPackage}from'@/lib/admin/types';
export default async function Page(){const[menus,packages]=await Promise.all([select<QrMenu>('qr_menus','select=*&order=created_at.desc&limit=200'),select<QrPackage>('qr_packages','select=*&active=eq.true&order=setup_price.asc')]);const live=menus.filter(m=>m.status==='yayinda').length;return <><header className="admin-head"><div><h1>QR Menü Merkezi</h1><p>Müşteriyi, paketi, tasarımı ve yıllık operasyonu tek merkezden yönet.</p></div><div className="admin-note">{live} yayında · {menus.length} toplam menü</div></header><section className="admin-card"><h2>Yeni QR Menü oluştur</h2><form action={createQrMenuConfiguredAction} className="admin-form"><label>İşletme adı<input name="business_name" required placeholder="Örn. Luna Cafe"/></label><label>Paket<select name="package_name" required><option value="">Paket seç</option>{packages.map(p=><option key={p.package_name} value={p.package_name}>{p.package_name} · {money(p.setup_price)} · Yenileme {money(p.renewal_price)}</option>)}</select></label><label>Şablon<select name="template_name" defaultValue="luks"><option value="minimal">Minimal</option><option value="luks">Lüks</option><option value="modern">Modern</option><option value="dogal">Doğal</option></select></label><label>Özel menü adresi<input name="slug" placeholder="luna-cafe"/></label><label>Ana renk<input name="primary_color" type="color" defaultValue="#171717"/></label><label>Instagram<input name="instagram" placeholder="@hesapadi"/></label><label>WhatsApp<input name="whatsapp" placeholder="905xx..."/></label><label>Adres<input name="address" placeholder="İşletme adresi"/></label><label>Wi-Fi adı<input name="wifi_name" placeholder="Müşteri Wi-Fi"/></label><label>Wi-Fi şifresi<input name="wifi_password" placeholder="İsteğe bağlı"/></label><div className="admin-span-2"><button className="admin-btn">QR Menü Kaydını Oluştur</button></div></form><div className="admin-note" style={{marginTop:14}}>Yeni menü taslak olarak oluşturulur. İçerik tamamlanmadan yayına alınamaz.</div></section><section className="admin-section"><div className="admin-section-title"><h2>QR Menü Müşterileri</h2><Link href="/admin/ayarlar" className="admin-note">Paket fiyatlarını yönet →</Link></div>{menus.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>İşletme</th><th>Menü Adresi</th><th>Paket / Şablon</th><th>İlk Satış</th><th>Ürün</th><th>Güncelleme</th><th>Yenileme</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{menus.map(m=><tr key={m.id}><td><b>{m.business_name}</b></td><td>/menu/{m.slug}</td><td>{m.package_name}<div className="admin-note">{m.template_name}</div></td><td>{money(m.setup_paid)} / {money(m.setup_price)}</td><td>{m.product_limit??'Sınırsız'}</td><td>{m.update_allowance==null?'Sınırsız':`${m.updates_used}/${m.update_allowance}`}</td><td>{new Date(m.renews_at).toLocaleDateString('tr-TR')}<div className="admin-note">{money(m.renewal_price)}</div></td><td><span className="admin-badge">{m.status}</span></td><td><div className="admin-actions"><Link className="admin-btn admin-btn-secondary" href={`/admin/qr-menu/${m.id}`}>İçeriği Yönet</Link><Link className="admin-btn admin-btn-secondary" href={`/admin/qr-menu/${m.id}/operasyon`}>QR & Finans</Link><form action={deleteQrMenuAction}><input type="hidden" name="id" value={m.id}/><ConfirmSubmitButton label="Sil" confirmText={`${m.business_name} QR menüsünü kalıcı olarak silmek istediğinize emin misiniz? Bu işlem geri alınamaz.`}/></form></div></td></tr>)}</tbody></table></div>:<div className="admin-empty"><div><b>Henüz QR Menü müşterisi yok</b><p>İlk işletmeyi eklediğinde içerik, QR kod, tahsilat ve yenileme takibi burada başlayacak.</p></div></div>}</section></>}
