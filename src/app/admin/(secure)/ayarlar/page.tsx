import{updatePackageAction,updatePricingSettingsAction}from'@/lib/admin/actions';import{select}from'@/lib/admin/db';import{getPricingSettings,money}from'@/lib/admin/pricing';import type{ServicePackage}from'@/lib/admin/types';export default async function Page(){const[p,pricing]=await Promise.all([select<ServicePackage>('service_packages','select=*&order=service_name.asc,package_name.asc'),getPricingSettings()]);return <><header className="admin-head"><div><h1>Ayarlar</h1><p>Fiyat motoru ve hizmet paketleri. Buradaki değişiklikler yeni tekliflere otomatik uygulanır.</p></div></header><section className="admin-card"><h2>Fiyat motoru</h2><form action={updatePricingSettingsAction} className="admin-form"><label>Ücretsiz tek yön mesafe (km)<input name="free_km" type="number" min="0" step="0.1" defaultValue={pricing.freeKm}/></label><label>Müşteriye ek km ücreti (TL)<input name="customer_km_rate" type="number" min="0" step="0.01" defaultValue={pricing.customerKmRate}/></label><label>Araç maliyeti (TL/km)<input name="vehicle_km_cost" type="number" min="0" step="0.01" defaultValue={pricing.vehicleKmCost}/></label><label>Ekipman payı (TL)<input name="equipment_cost" type="number" min="0" step="0.01" defaultValue={pricing.equipmentCost}/></label><label>Kapora oranı (%)<input name="deposit_rate" type="number" min="0" max="100" step="1" defaultValue={pricing.depositRate*100}/></label><div className="admin-span-2"><button className="admin-btn">Fiyat Kurallarını Kaydet</button></div></form></section><section className="admin-section"><h2>Hizmet paketleri</h2><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Hizmet</th><th>Paket</th><th>Satış</th><th>İşçilik</th><th>Aktif</th><th></th></tr></thead><tbody>{p.map(x=><tr key={x.id}><td>{x.service_name}</td><td>{x.package_name}</td><td colSpan={4}><form action={updatePackageAction} className="admin-form-grid"><input type="hidden" name="id" value={x.id}/><input name="price" type="number" min="0" step="0.01" defaultValue={x.price} aria-label="Satış fiyatı"/><input name="labor_cost" type="number" min="0" step="0.01" defaultValue={x.labor_cost} aria-label="İşçilik maliyeti"/><label><input type="checkbox" name="active" defaultChecked={x.active}/> Aktif</label><button className="admin-btn admin-btn-secondary">Kaydet</button></form><div className="admin-note">Mevcut: {money(x.price)} · İşçilik {money(x.labor_cost)}</div></td></tr>)}</tbody></table></div></section></>}
