import{addPaymentAction,updateJobAction}from'@/lib/admin/actions';import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{Job}from'@/lib/admin/types';const states=['planlandi','cekim_tamamlandi','kurguda','teslime_hazir','teslim_edildi','iptal'];const stateLabels:Record<string,string>={planlandi:'Planlandı',cekim_tamamlandi:'Çekim Tamamlandı',kurguda:'Kurguda',teslime_hazir:'Teslime Hazır',teslim_edildi:'Teslim Edildi',iptal:'İptal'};export default async function Page(){const jobs=await select<Job>('jobs','select=*&order=scheduled_at.desc&limit=200');return <><header className="admin-head"><div><h1>İş Takibi</h1><p>Planlamadan teslimata, kaporadan kalan ödemeye bütün işler.</p></div></header><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Müşteri</th><th>Çekim</th><th>Teslim</th><th>Ücret</th><th>Durum</th><th>Ödeme ekle</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td>{j.customer_name}<div className="admin-note">{j.service_name}</div></td><td>{new Date(j.scheduled_at).toLocaleString('tr-TR')}</td><td>{j.delivery_at?new Date(j.delivery_at).toLocaleString('tr-TR'):'—'}</td><td>{money(j.total_price)}<div className="admin-note">Alınan {money(j.paid_amount)} · Kalan {money(j.remaining_amount)}</div></td><td><form action={updateJobAction} className="admin-stack"><input type="hidden" name="id" value={j.id}/><input type="hidden" name="total_price" value={j.total_price}/><input type="hidden" name="paid_amount" value={j.paid_amount}/><select name="job_status" defaultValue={j.job_status}>{states.map(s=><option value={s} key={s}>{stateLabels[s]}</option>)}</select><input name="delivery_at" type="datetime-local" defaultValue={j.delivery_at?.slice(0,16)}/><button className="admin-btn admin-btn-secondary">Kaydet</button></form></td><td><form action={addPaymentAction} className="admin-stack"><input type="hidden" name="job_id" value={j.id}/><input name="amount" type="number" min="0.01" step="0.01" placeholder="Tutar" required/><select name="payment_type" defaultValue={j.paid_amount>0?'kalan':'kapora'}><option value="kapora">Kapora</option><option value="kalan">Kalan ödeme</option><option value="diger">Diğer</option></select><select name="payment_method" defaultValue=""><option value="">Ödeme şekli</option><option value="nakit">Nakit</option><option value="havale">Havale/EFT</option><option value="kart">Kart</option></select><button className="admin-btn">Ödeme Kaydet</button></form></td></tr>)}</tbody></table></div></>}
