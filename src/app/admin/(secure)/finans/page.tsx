import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{Job}from'@/lib/admin/types';export default async function Page(){const jobs=(await select<Job>('jobs','select=*&order=scheduled_at.desc&limit=500')).filter(j=>j.job_status!=='iptal'),ciro=jobs.reduce((s,j)=>s+Number(j.total_price),0),paid=jobs.reduce((s,j)=>s+Number(j.paid_amount),0),rem=jobs.reduce((s,j)=>s+Number(j.remaining_amount),0);return <><header className="admin-head"><div><h1>Finans</h1><p>Ciro, tahsilat, kapora ve kalan alacak.</p></div></header><div className="admin-grid"><div className="admin-card"><h3>Ciro</h3><div className="admin-kpi">{money(ciro)}</div></div><div className="admin-card"><h3>Tahsil</h3><div className="admin-kpi">{money(paid)}</div></div><div className="admin-card"><h3>Kalan Alacak</h3><div className="admin-kpi">{money(rem)}</div></div><div className="admin-card"><h3>Açık İş</h3><div className="admin-kpi">{jobs.filter(j=>Number(j.remaining_amount)>0).length}</div></div></div></>}
