import Link from'next/link';import{createDigitalBrandAction}from'@/lib/admin/digital-brand-actions';import{select}from'@/lib/admin/db';
type Row={id:string;name:string;slug:string;qr_name:string;status:'taslak'|'yayinda'|'pasif';logo_url:string|null;created_at:string};
export default async function Page(){const rows=await select<Row>('digital_brand_pages','select=*&order=created_at.desc');return <><header className="admin-head"><div><h1>Dijital Marka Sayfaları</h1><p>Yeni müşteriyi ekle, sayfasını düzenle, QR adını belirle ve yayını tek merkezden yönet.</p></div><div className="admin-note">{rows.filter(r=>r.status==='yayinda').length} yayında · {rows.length} toplam</div></header><section className="admin-card"><h2>Yeni marka sayfası oluştur</h2><form action={createDigitalBrandAction} className="admin-form"><label>Marka adı<input name="name" required placeholder="Örn. Elosy Shoes"/></label><label>Sayfa adresi<input name="slug" placeholder="elosy-shoes"/></label><label>QR adı<input name="qr_name" placeholder="Kamerada/QR kartında kullanacağın ad"/></label><label>Üst küçük başlık<input name="eyebrow" placeholder="ELOSY SHOES"/></label><label className="admin-span-2">Ana başlık<input name="title" placeholder="Markaya özel başlık"/></label><label className="admin-span-2">Açıklama<input name="description" placeholder="Kısa marka açıklaması"/></label><label>Ana renk<input name="accent" type="color" defaultValue="#171717"/></label><label>Alt not<input name="footer_note" placeholder="© Marka Adı"/></label><div className="admin-span-2"><button className="admin-btn">Marka Sayfasını Oluştur</button></div></form></section><section className="admin-section"><h2>Markalar</h2>{rows.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Marka</th><th>Adres</th><th>QR adı</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.name}</b></td><td>/dijital/{r.slug}</td><td>{r.qr_name}</td><td><span className="admin-badge">{r.status}</span></td><td><div className="admin-actions"><Link className="admin-btn admin-btn-secondary" href={`/admin/dijital-markalar/${r.id}`}>Düzenle</Link><a className="admin-btn admin-btn-secondary" href={`/dijital/${r.slug}`} target="_blank" rel="noreferrer">Görüntüle</a></div></td></tr>)}</tbody></table></div>:<div className="admin-empty"><div><b>Henüz marka sayfası yok</b><p>İlk müşteriyi yukarıdaki formdan oluşturabilirsin.</p></div></div>}</section></>}
