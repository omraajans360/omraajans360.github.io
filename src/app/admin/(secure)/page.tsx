import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{Job,QrMenu}from'@/lib/admin/types';
export default async function Page(){const[jobs,menus]=await Promise.all([select<Job>('jobs','select=*&order=scheduled_at.desc&limit=100'),select<QrMenu>('qr_menus','select=*&order=renews_at.asc&limit=50')]),active=jobs.filter(j=>!['teslim_edildi','iptal'].includes(j.job_status)),revenue=jobs.filter(j=>j.job_status!=='iptal').reduce((s,j)=>s+Number(j.total_price),0),collected=jobs.reduce((s,j)=>s+Number(j.paid_amount),0),receivable=jobs.reduce((s,j)=>s+Number(j.remaining_amount),0),today=new Date().toISOString().slice(0,10),todayJobs=jobs.filter(j=>j.scheduled_at?.slice(0,10)===today),todayDeliveries=jobs.filter(j=>j.delivery_at?.slice(0,10)===today&&j.job_status!=='teslim_edildi');return <><header className="admin-head"><div><h1>Yönetim Özeti</h1><p>Ajans işleri, tahsilatlar, teslimler ve QR Menü tek ekranda.</p></div></header><div className="admin-grid"><div className="admin-card"><h3>Aktif İş</h3><div className="admin-kpi">{active.length}</div></div><div className="admin-card"><h3>Toplam Ciro</h3><div className="admin-kpi">{money(revenue)}</div></div><div className="admin-card"><h3>Tahsil Edilen</h3><div className="admin-kpi">{money(collected)}</div></div><div className="admin-card"><h3>Kalan Alacak</h3><div className="admin-kpi">{money(receivable)}</div></div></div><section className="admin-section admin-grid"><div className="admin-card"><h3>Bugün Çekilecek</h3><div className="admin-kpi">{todayJobs.length}</div></div><div className="admin-card"><h3>Bugün Teslim</h3><div className="admin-kpi">{todayDeliveries.length}</div></div><div className="admin-card"><h3>Aktif QR Menü</h3><div className="admin-kpi">{menus.filter(m=>m.status==='yayinda').length}</div></div><div className="admin-card"><h3>Toplam QR Menü</h3><div className="admin-kpi">{menus.length}</div></div></section><section className="admin-section"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Tarih</th><th>Müşteri</th><th>Hizmet</th><th>Durum</th><th>Teslim</th><th>Kalan</th></tr></thead><tbody>{active.slice(0,10).map(j=><tr key={j.id}><td>{new Date(j.scheduled_at).toLocaleString('tr-TR')}</td><td>{j.customer_name}</td><td>{j.service_name}</td><td><span className="admin-badge">{j.job_status}</span></td><td>{j.delivery_at?new Date(j.delivery_at).toLocaleDateString('tr-TR'):'—'}</td><td>{money(j.remaining_amount)}</td></tr>)}</tbody></table></div></section></>}
