import{addQrSetupPaymentAction}from'@/lib/admin/qr-admin-actions';import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{QrMenu}from'@/lib/admin/types';type Row={id:string;amount:number;payment_method:string|null;paid_at:string;notes:string|null};export async function QrSetupPaymentPanel({menu}:{menu:QrMenu}){const rows=await select<Row>('qr_setup_payments',`qr_menu_id=eq.${encodeURIComponent(menu.id)}&select=*&order=paid_at.desc&limit=30`),remaining=Math.max(0,Number(menu.setup_price)-Number(menu.setup_paid));return <section className="admin-card"><h2>İlk Paket Satışı</h2><div className="admin-grid"><div><div className="admin-note">Paket fiyatı</div><div className="admin-kpi">{money(menu.setup_price)}</div></div><div><div className="admin-note">Tahsil</div><div className="admin-kpi">{money(menu.setup_paid)}</div></div><div><div className="admin-note">Kalan</div><div className="admin-kpi">{money(remaining)}</div></div></div>{remaining>0?<form action={addQrSetupPaymentAction} className="admin-form"><input type="hidden" name="menu_id" value={menu.id}/><label>Tahsilat<input name="amount" type="number" min="0.01" max={remaining} step="0.01" required/></label><label>Ödeme şekli<select name="payment_method" defaultValue="havale"><option value="havale">Havale/EFT</option><option value="nakit">Nakit</option><option value="kart">Kart</option></select></label><label className="admin-span-2">Not<input name="notes"/></label><div className="admin-span-2"><button className="admin-btn">Tahsilatı Kaydet</button></div></form>:<p className="admin-badge">İlk paket ödemesi tamamlandı.</p>}{rows.length>0&&<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Tarih</th><th>Tutar</th><th>Şekil</th><th>Not</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.paid_at).toLocaleString('tr-TR')}</td><td>{money(r.amount)}</td><td>{r.payment_method||'—'}</td><td>{r.notes||'—'}</td></tr>)}</tbody></table></div>}</section>}
