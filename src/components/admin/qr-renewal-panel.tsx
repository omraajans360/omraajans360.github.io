import{renewQrMenuAction}from'@/lib/admin/qr-renewal-actions';import{select}from'@/lib/admin/db';import{money}from'@/lib/admin/pricing';import type{QrMenu}from'@/lib/admin/types';type Renewal={id:string;qr_menu_id:string;amount:number;payment_method:string|null;period_start:string;period_end:string;paid_at:string;notes:string|null};export async function QrRenewalPanel({menu}:{menu:QrMenu}){const renewals=await select<Renewal>('qr_renewals',`qr_menu_id=eq.${encodeURIComponent(menu.id)}&select=*&order=paid_at.desc&limit=20`);return <section className="admin-card"><h2>Yıllık Yenileme</h2><p className="admin-note">Sonraki yenileme: {new Date(menu.renews_at).toLocaleDateString('tr-TR')} · Güncel ücret {money(menu.renewal_price)}</p><form action={renewQrMenuAction} className="admin-form"><input type="hidden" name="menu_id" value={menu.id}/><label>Tahsil edilen yenileme ücreti<input name="amount" type="number" min="0.01" step="0.01" defaultValue={menu.renewal_price} required/></label><label>Ödeme şekli<select name="payment_method" defaultValue="havale"><option value="havale">Havale/EFT</option><option value="nakit">Nakit</option><option value="kart">Kart</option></select></label><label className="admin-span-2">Not<input name="notes"/></label><div className="admin-span-2"><button className="admin-btn">Yenilemeyi ve Tahsilatı Kaydet</button></div></form>{renewals.length>0&&<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Ödeme</th><th>Dönem</th><th>Tutar</th><th>Şekil</th></tr></thead><tbody>{renewals.map(r=><tr key={r.id}><td>{new Date(r.paid_at).toLocaleDateString('tr-TR')}</td><td>{new Date(r.period_start).toLocaleDateString('tr-TR')} → {new Date(r.period_end).toLocaleDateString('tr-TR')}</td><td>{money(r.amount)}</td><td>{r.payment_method||'—'}</td></tr>)}</tbody></table></div>}</section>}
