'use server';
import{revalidatePath}from'next/cache';import{requireAdmin}from'./auth';import{select,update}from'./db';import{uploadQrMedia}from'./storage';import type{MenuProduct,QrMenu}from'./types';
const text=(f:FormData,k:string)=>String(f.get(k)??'').trim();
async function menu(id:string){const m=(await select<QrMenu>('qr_menus',`id=eq.${encodeURIComponent(id)}&select=*`))[0];if(!m)throw new Error('QR Menü bulunamadı.');return m}
async function refresh(id:string){const m=await menu(id);revalidatePath(`/admin/qr-menu/${id}`);revalidatePath('/admin/qr-menu');revalidatePath(`/menu/${m.slug}`)}
export async function uploadMenuBrandMediaAction(f:FormData){await requireAdmin();const id=text(f,'menu_id'),kind=text(f,'kind');if(!['logo','cover'].includes(kind))throw new Error('Geçersiz görsel türü.');const file=f.get('file');if(!(file instanceof File))throw new Error('Görsel seçilmedi.');const url=await uploadQrMedia(file,id,kind as'logo'|'cover');await update('qr_menus',`id=eq.${encodeURIComponent(id)}`,{[kind==='logo'?'logo_url':'cover_url']:url,updated_at:new Date().toISOString()});await refresh(id)}
export async function uploadProductMediaAction(f:FormData){await requireAdmin();const menuId=text(f,'menu_id'),productId=text(f,'product_id'),p=(await select<MenuProduct>('menu_products',`id=eq.${encodeURIComponent(productId)}&menu_id=eq.${encodeURIComponent(menuId)}&select=*`))[0];if(!p)throw new Error('Ürün bulunamadı.');const file=f.get('file');if(!(file instanceof File))throw new Error('Görsel seçilmedi.');const url=await uploadQrMedia(file,menuId,'product');await update('menu_products',`id=eq.${encodeURIComponent(productId)}`,{image_url:url});await refresh(menuId)}
